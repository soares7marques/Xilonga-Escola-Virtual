spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/xilongaBD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DATABASE_USER:postgres}
spring.datasource.password=${DATABASE_PASSWORD:xilonga123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
logging.level.org.springframework.security=DEBUG
app.jwt.secret=${JWT_SECRET:xilonga-secret-key-jwt-precisa-ter-mais-de-32-caracteres}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
app.upload.aulas-dir=uploads/aulas
spring.servlet.multipart.max-file-size=30MB
spring.servlet.multipart.max-request-size=30MB
server.port=${PORT:8080}
